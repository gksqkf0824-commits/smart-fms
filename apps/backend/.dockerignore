# Docker 빌드 컨텍스트에서 제외할 것들 (.gitignore의 Docker 버전)
# 이미지에 들어가면 안 되거나(비밀), 넣을 필요 없는(빌드 산출물) 파일들

# 빌드 산출물 — 이미지 안에서 새로 빌드하므로 로컬 것은 불필요
build/
.gradle/
bin/
out/

# 비밀 정보 — 이미지에 박히면 이미지 받는 사람 모두에게 유출됨
.env
*.env

# IDE·OS 잡동사니
.idea/
.vscode/
*.iml
.DS_Store

# 로그
*.log
