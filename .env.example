# 실제 값은 .env에 넣습니다 (.env는 git에 올라가지 않음)
# S3를 쓰지 않는 팀원은 이 파일을 복사할 필요 없이 그냥 실행하면 됩니다 (경로만 생성 모드)

# S3 실제 업로드를 켤 때만 true
APP_S3_ENABLED=true

AWS_S3_BUCKET=smart-fms-inspections-evida
AWS_REGION=ap-northeast-2

# IAM 사용자(smart-fms-backend)의 액세스 키 — 절대 커밋 금지
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
